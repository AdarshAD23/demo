name: test_project
on: push
jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Setup Nodejs
        uses: actions/setup-node@v3
        with:
          node-version: 18
        - run: npm ci
        - run: npm test